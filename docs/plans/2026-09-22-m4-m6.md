# M4–M6 Plan — Ameliorations · Refactor · Features

> **For opencode:** REQUIRED SUB-SKILL: superpowers:executing-plans.
> **Dependances:** M1–M3 fermes, `python run_all.py` 17/17.

---

### M4 — Qualite scientifique & performance

#### Task 11: Temperature scaling du LR gate (ECE 0.4178 -> <0.20)
- Files: `eval_harness.py`, `intuition.py`
- Step 1: apres `fit_lr`, calculer `T = argmin NLL(T)` par golden-section sur [0.1, 3.0] (NLL = -[y ln p + (1-y) ln(1-p)]/n, `p = sigmoid(z/T)`).
- Step 2: stocker `T` dans `LogisticGate`; `predict_instant_proba` divise le z par `T`.
- Step 3: test assertif: `ece_lr < ece_prev * 1.05` (tolerance).
- Commit: `feat: temperature scaling lr gate`

#### Task 12: Stats cache LRU (hit-rate > 0 en pratique)
- Files: `intuition.py`
- Step 1: `features_from_text` est deja `@lru_cache`. Ajouter un `get_stats()` qui retourne `features_from_text.cache_info()` (stdlib tuple).
- Step 2: dans `IntuitionInstant.route_batch`, apres la boucle, si `hits + misses > 0` imprimer hit-ratio (une seule fois via flag `_stats_printed`).
- Step 3: `test_intuition.py` ajoute 1 assert: apres 2 route_batch sur les 9 prompts du corpus, `misses == 9` et `hits == 9`.
- Commit: `feat: lru cache stats`

#### Task 13: Route_batch — features precomputees une fois (dedup 2×)
- Files: `intuition.py` `route_batch` (ligne 369–387)
- Bug actuel: `f` est extrait ligne 374 (float64), `X` ligne 370 (float32) — double extraction par element. Fix: ligne 370 `X = np.array(...)`; ligne 374 `f = np.asarray(X[i], np.float64)` par index au lieu de re-extraire.
- Verification: `test_intuition.py` — meme checksums qu'avant (parite de la table).
- Commit: `fix: route_batch features 1x`

#### Task 14: Parite 89 ids dans `test_showcase.js`
- Files: `test_showcase.js`
- Etendre la liste des ids testees a 20 champions + quantum (deja partiellement fait).
- Assertif: max_rel_err < 1e-4 par id.
- Commit: `test: parite JS 20 kernels`

---

### M5 — Refactor & typage

#### Task 15: Enlever `no-untyped-def` — annoter tous les champions
- Files: `mypy.ini`, `intuition.py`, `champions_extra.py`
- Enlever `no-untyped-def` de `disable_error_code` dans `mypy.ini` (ligne 3); annoter chaque def de champion (`def tanh_pade(x: Any) -> npt.NDArray[Any]` ou `-> float` selon le cas).
- Verification: `python typecheck.py` -> 0 erreurs.
- Commit: `refactor: annotations champions, mypy strict complet`

#### Task 16: Extraire `spear_fable.py` (modularite)
- Files: nouveau `spear_fable.py` (SpearEmb + TinyPolicy + LogisticGate + `_lr_from_corpus`); `intuition.py` les importe depuis ce module; `mypy.ini` + `run_all.py` CMDS mis a jour.
- Verification: `python run_all.py` 17/17; checksums `test_reports.py` inchange.
- Commit: `refactor: split spear_fable`

#### Task 17: Docstrings numpy-style sur toutes les classes publiques
- Files: `spear_fable.py` (nouveau), `intuition.py`
- 4 classes: `SpearEmb`, `TinyPolicy`, `LogisticGate`, `IntuitionInstant`. Format `Parameters / Returns` concis.
- Verification: lecture seule + `python -c "import intuition; help(intuition.IntuitionInstant)"` sans erreur.
- Commit: `docs: docstrings numpy-style`

#### Task 18: `__all__` explicite dans `intuition.py`
- Lister ~35 noms publics (champions, classes, QUANTUM, CHAMPIONS, FAST, features_from_text, demo, export_weights).
- Verification: `python -c "import intuition; print(len(intuition.__all__))"` = attendu.
- Commit: `refactor: __all__ intuition`

#### Task 19: `cov.py` -> 2 seuils par fichier (intuition > 0.85, spear_fable > 0.90)
- Files: `cov.py`
- Ajouter un dict `THRESHOLDS = {"intuition.py": 0.85, "spear_fable.py": 0.90, ...}`; lever `SystemExit` si un des seuils est en dessous.
- Commit: `test: seuils coverage par module`

---

### M6 — Features & use cases

#### Task 20: Schema validation des JSON reports (`schema.py`)
- Nouveau fichier `schema.py` (~40 lignes): `validate(obj: dict, schema: dict) -> list[str]` — recursive, stdlib only; schemas: `eval_report.json`, `e2e_report.json`, `wasm_bench_report.json`, `wasm_batch_report.json`, `slm-weights.json`.
- Integrer en premiere ligne de chaque `test_*.py` (ou dans `test_reports.py`).
- Verification: `python test_reports.py` OK apres `python run_all.py`.
- Commit: `feat: schema validation json reports`

#### Task 21: Quantization int8 des poids SLM (taille -55%)
- Files: nouveau `quantize.py`; `showcase.html` (bloc embed) via `gen_showcase.py`.
- Impl: `q = np.round(w * 1000).astype(np.int32)` (scale 1000 = 3 decimales); decompress: `w = q.astype(np.float32) / 1000.0`.
- `gen_showcase.py` ecrit les poids quantizes dans le bloc `<script id="slm">`; `slm-weights.json` reste la version float.
- Verification: `node test_showcase.js` — parite 4 scenarios ± 5e-4.
- Commit: `feat: int8 quantization slm`

#### Task 22: Invariants par fuzz (3 regles, `test_properties.py`)
- Invariants sur les 9 prompts du corpus + 10 generes:
  1. `dim(f) == 16` toujours.
  2. `0 <= p <= 1` et `0 <= conf <= 1`.
  3. `route(t).path == route_batch([t])[0].path` (parite scalar/batch).
- Fuzz: 10 phrases aleatoires (`" ".join(rng.choice(MOTS, k=rng.randint(1,15)))`).
- Commit: `test: invariants par fuzz`

#### Task 23: CLI unifie dans `intuition.py` (4 modes)
- Modes: `--demo` (defaut), `--route "texte"`, `--bench` (= run_all), `--export [--path PATH]`.
- Parsing `argparse` stdlib.
- Verification: 4 commandes → exit 0.
- Commit: `feat: cli intuition`

#### Task 24: `readme_sync.py` — injection automatique des chiffres REPORT.json
- Lit `REPORT.json`, in-place patch de `README.md` entre balises `<!-- BEGIN N -->`/`<!-- END N -->` (3 blocs: gate table, e2e line, wasm line).
- Integrer dans `run_all.py` apres `make_report.py`.
- Verification: `python readme_sync.py` → README identique (idempotence, 2 runs).
- Commit: `feat: readme sync automatique`

#### Task 25: Demo integration mini-agent (`demo_agent.py`)
- 3 prompts typiques (court, code, math).
- Affiche pour chaque: `path`, `p`, `conf`, `complexity`, temps us.
- `run_all.py` intègrera la sortie de `python demo_agent.py` en etape 18.
- Commit: `feat: demo mini-agent`

---

## Checkpoint final

```
python run_all.py          → 25+/25+ etapes OK
python make_report.py      → REPORT status=GREEN tests_ok=N/N
node test_showcase.js      → ALL GREEN
gh run list                → matrice success (windows/ubuntu × 3.10/3.12)
python readme_sync.py      → idempotent, 0 diff
```

## Notes de mise a jour

- Apres chaque task: commit immediat (`feat: ...`, `test: ...`, `refactor: ...`, `fix: ...`).
- Fermer l'issue correspondante via `gh api -X PATCH .../issues/N -f state=closed`.
- Les 15 issues #11–#25 → milestones M4/M5/M6; chaque milestone ferme quand ses issues sont toutes vertes.
- Le `README.md` sera regeneré par `readme_sync.py` (Task 24) et doit rester ≤80 lignes.